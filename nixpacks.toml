providers = ["node"]

[node]
version = "22"

[phases.install]
cmds = [
  "cd backend && npm ci",
  "node backend/setup_uploads_directories.js"
]

[phases.build]
cmds = ["echo 'Build skipped'"]

[start]
cmd = "npm start"