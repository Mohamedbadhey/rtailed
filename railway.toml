[build]
builder = "nixpacks"

[deploy]
startCommand = "NODE_ENV=production npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.variables]
DB_WAIT_MAX_MS = "30000"
